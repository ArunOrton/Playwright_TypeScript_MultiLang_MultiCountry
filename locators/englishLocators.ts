
import { commonLocators } from './commonLocators'
import { LocatorType } from './ICountryLocator'

export const englishLocators: LocatorType  = {
  ...commonLocators,
  cookiesConsentYes: 'Allow all cookies',
  goToTop: 'Go to top of page',
  countryName: 'English',
}